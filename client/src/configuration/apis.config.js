const configuration = {
  protocol: "http",
  host: "localhost",
  port: 5000,
};
const apis_base_url = `${configuration.protocol}://${configuration.host}:${configuration.port}`;
export default apis_base_url;
